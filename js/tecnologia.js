const ir_topo = document.querySelector("#ir_topo");

ir_topo.addEventListener("click", function () {
window.scrollTo({
	//rodar a pagina até:
		top:0,
		left:0,
		behavior:"smooth"
    })        
});